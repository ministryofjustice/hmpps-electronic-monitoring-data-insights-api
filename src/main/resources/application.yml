info.app:
  name: HMPPS Electronic Monitoring Data Insights Api
  version: 1.0

spring:
  application:
    name: hmpps-electronic-monitoring-data-insights-api
  jackson:
    date-format: "yyyy-MM-dd HH:mm:ss"

  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${hmpps-auth.url}/.well-known/jwks.json

      client:
        provider:
          hmpps-auth:
            token-uri: ${hmpps-auth.url}/oauth/token
        registration:
          person-record-api:
            provider: hmpps-auth
            client-id: ${person-record-api.client.id}
            client-secret: ${person-record-api.client.secret}
            authorization-grant-type: client_credentials
            scope: read
          access-control-api:
            provider: hmpps-auth
            client-id: ${probation-access-control-api.client.id}
            client-secret: ${probation-access-control-api.client.secret}
            authorization-grant-type: client_credentials
            scope: read
  http:
    codecs:
      max-in-memory-size: 10MB
  web:
    error:
      include-message: always

server:
  port: 8080
  servlet:
    context-path: /
  forward-headers-strategy: native
  tomcat:
    remoteip:
      remote-ip-header: x-forwarded-for
      protocol-header: x-forwarded-proto
      internal-proxies: 10\.\d{1,3}\.\d{1,3}\.\d{1,3}|192\.168\.\d{1,3}\.\d{1,3}|169\.254\.\d{1,3}\.\d{1,3}|127\.\d{1,3}\.\d{1,3}\.\d{1,3}|172\.1[6-9]{1}\.\d{1,3}\.\d{1,3}|172\.2[0-9]{1}\.\d{1,3}\.\d{1,3}|172\.3[0-1]{1}\.\d{1,3}\.\d{1,3}|0:0:0:0:0:0:0:1|::1|100\.6[4-9]\.\d{1,3}\.\d{1,3}|100\.[7-9][0-9]{1}\.\d{1,3}\.\d{1,3}|100\.1[0-1][0-9]{1}\.\d{1,3}\.\d{1,3}|100\.12[0-7]\.\d{1,3}\.\d{1,3}
  shutdown: graceful

aws:
  region: eu-west-2

  athena:
    defaultDatabase: ${MDSS_DB:data_insights_dev_dbt}
    mdssDatabase: ${MDSS_DB:data_insights_dev_dbt}
    fmsDatabase: ${FMS_DB:serco_fms_curated_dev_dbt}
    outputLocation: ${ATHENA_OUTPUT_BUCKET:s3://emds-dev-athena-query-results-20240917144028307600000004/output}
    workgroup: ${WORKGROUP:800964199911_default}
    rowLimit: ${ROW_LIMIT:1000}
    pollIntervalMs: 500
    timeoutMs: ${ATHENA_TIMEOUT_MS:120000}
    role: ${ATHENA_GENERAL_IAM_ROLE:}
    responsibleOrganisations: ${RESPONSIBLE_ORGANISATIONS:}

management:
  endpoints:
    web:
      base-path: /
      exposure:
        include: 'info, health'
  endpoint:
    health:
      cache:
        time-to-live: 2000ms
      show-components: always
      show-details: always
      probes:
        enabled: true
    info:
      cache:
        time-to-live: 2000ms

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

service:
  base-url: ${SERVICE_BASE_URL}
  ui-base-url: ${UI_SERVICE_BASE_URL}
  status:
    cache-timeout: ${SERVICE_STATUS_CACHE_TIMEOUT:5m}
    data-out-of-sync-threshold-minutes: ${SERVICE_STATUS_DATA_OUT_OF_SYNC_THRESHOLD_MINUTES:15}

cpr:
  enabled: ${CPR_ENABLED:false}

access-control:
  enabled: ${ACCESS_CONTROL_ENABLED:false}

slack:
  webhook-url: ${SLACK_WEBHOOK_URL:}

status-alert:
  enabled: ${STATUS_ALERT_ENABLED:false}

apis:
  person-record-api:
    url: ${PERSON_RECORD_API_URL}
  access-control-api:
    url: ${ACCESS_CONTROL_API_URL}
