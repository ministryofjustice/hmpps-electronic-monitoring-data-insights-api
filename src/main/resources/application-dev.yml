hmpps-auth:
  url: "https://sign-in-dev.hmpps.service.justice.gov.uk/auth"
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/em_data_insights
    username: hmpps
    password: hmpps

aws:
  region: eu-west-2
  athena:
    defaultDatabase: staged_mdss_dev_dbt
    mdssDatabase: staged_mdss_dev_dbt
    fmsDatabase: serco_fms_curated_dev_dbt
    outputLocation: s3://emds-dev-athena-query-results-20240917144028307600000004/output
    workgroup: 800964199911_default
    rowLimit: ${ROW_LIMIT:1000}
    pollIntervalMs: 500
    timeoutMs: 60000
    role: ${ATHENA_GENERAL_IAM_ROLE:}

service:
  base-url: http://localhost:8081
  ui-base-url: http://localhost:8081